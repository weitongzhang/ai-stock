# stock_selection

Future home for strategy engines and reports.

Initial target:

- Wrap YC-buy `BuyPointAnalyzer` and `TripleScreenSystem`.
- Accept normalized OHLCV from `market_data`.
- Add scoring, risk filters, and CSV/Markdown reports.
