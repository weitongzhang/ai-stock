# stock_analysis

Reusable services for individual stock signals, YC-buy adapter output, BSA
structure checks, key levels, and invalidation rules.

This layer should consume normalized OHLCV data and return structured
`StockSignal` objects.

